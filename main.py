from tkinter import *

root = Tk()

root.title("Library Management System")
root.geometry("800x500")

title = Label(
    root,
    text="Library Management System",
    font=("Arial", 20, "bold")
)

title.pack(pady=50)

root.mainloop()
